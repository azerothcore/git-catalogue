import { EmojiFixupPipe } from './emoji-fixup.pipe';

describe('EmojiFixupPipe', () => {
  it('create an instance', () => {
    const pipe = new EmojiFixupPipe();
    expect(pipe).toBeTruthy();
  });
});
